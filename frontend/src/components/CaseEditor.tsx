export default function CaseEditor({ onSubmit }: { onSubmit: (value: { task_id: string; prompt: string }) => void }) { return <form onSubmit={event => { event.preventDefault(); const data = new FormData(event.currentTarget); onSubmit({ task_id: String(data.get("task_id")), prompt: String(data.get("prompt")) }); }}><label>TASK ID<input name="task_id" required /></label><label>PROMPT<textarea name="prompt" required /></label><button className="action">SAVE CASE</button></form>; }
