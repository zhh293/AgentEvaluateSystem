export default function EnvironmentDiff({ before, after }: { before: Record<string, unknown>; after: Record<string, unknown> }) { const keys = [...new Set([...Object.keys(before), ...Object.keys(after)])]; return <div className="code-block">{keys.map(key => before[key] === after[key] ? null : <p key={key}><b>{key}</b>: {JSON.stringify(before[key])} → {JSON.stringify(after[key])}</p>)}</div>; }
