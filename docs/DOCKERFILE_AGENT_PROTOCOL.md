# Superseded protocol

Dockerfile-first v1 has been replaced by the Compose-first project protocol.
See [AGENT_PROJECT_PROTOCOL.md](AGENT_PROJECT_PROTOCOL.md). Dockerfile remains a
supported single-service deployment, but `agent-eval.yaml` is always required.
