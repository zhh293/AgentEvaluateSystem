global:
  scrape_interval: 15s
rule_files: ["/etc/prometheus/alerts.yml"]
scrape_configs:
  - job_name: agent-eval-backend
    static_configs: [{ targets: ["backend:8000"] }]
  - job_name: prometheus
    static_configs: [{ targets: ["localhost:9090"] }]
