export default function FileUploader({ file, onChange }: { file: File | null; onChange: (file: File | null) => void }) { return <label className="dropzone"><input type="file" accept=".zip,.tgz,.gz" onChange={event => onChange(event.target.files?.[0] ?? null)} /><b>{file?.name ?? "DROP OR SELECT AGENT PROJECT"}</b><span>ZIP · TAR.GZ · TGZ / Dockerfile-first</span></label>; }
