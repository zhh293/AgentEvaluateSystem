export default function TraceTimeline({ spans, active, onSelect }: { spans: Array<Record<string, unknown>>; active: number; onSelect: (index: number) => void }) { return <div>{spans.map((span, index) => <button className={`span-row ${active === index ? "active" : ""}`} onClick={() => onSelect(index)} key={String(span.span_id ?? index)}>{String(span.operation ?? span.span_type ?? "span")}</button>)}</div>; }
