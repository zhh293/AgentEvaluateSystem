schema_version: 1
deployment:
  type: compose
  file: docker-compose.yml
  entry_service: agent
runtime:
  protocol: http
  port: 8080
  healthcheck: /health
  invoke: /v1/evaluations/run
  timeout_seconds: 300
  startup_timeout_seconds: 120
  state_scope: evaluation
security:
  network: none

